extends layout

block content
  h1= ingredient.name
  h2= ingredient.package
  h2= ingredient.temperature
  h2= ingredient.amount

  form(action='/ingredients/' + ingredient.name + '/delete', method='post')
    input(type='submit', value = 'Delete ingredient')